# Charts Sandbox

Interactive sandbox for [@gravity-ui/charts](https://github.com/gravity-ui/charts) library. Edit chart data (JSON) and configuration (TypeScript) in separate tabs, execute code in isolated sandboxes, and see live chart previews.

## Quick Start

```bash
npm install
npm run dev
```

Open http://localhost:5173
